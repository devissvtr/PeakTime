package com.devissvtr.peaktime.ui.note

import android.os.Bundle
import android.view.View
import androidx.activity.enableEdgeToEdge
import androidx.appcompat.app.AppCompatActivity
import androidx.core.view.ViewCompat
import androidx.core.view.WindowInsetsCompat
import com.devissvtr.peaktime.R
import com.devissvtr.peaktime.databinding.ActivityInputNoteBinding

class InputNoteActivity : AppCompatActivity() {
//    private lateinit var viewModel:...
//    private lateinit var binding:....
//    private lateinit var currentFile: File? = null

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

//        binding = ActivityInputNoteBinding.inflate(layoutInflater)
//        setContentView(binding.root)


    }

//    private fun setupListerners() {
//        binding.apply {
//            btnSubmit.setOnClickListerner { saveNote() }
//        }
//    }

//    private fun showLoad(isLoading: Boolean) {
//        binding.progressBar.visibility = if(isLoading) View.VISIBLE else View.GONE
//    }

//    private fun saveNote() {
//        showLoad(true)
//
//        if (currentFile != null) {
//            val title = binding.edtTitle.text.toString().toRequestBody("text/plain".toMediaType())
//            val description = binding.edtDescription.text.toString().toRequestBody("text/plain".toMediaType())
//
//        }
//    }
}